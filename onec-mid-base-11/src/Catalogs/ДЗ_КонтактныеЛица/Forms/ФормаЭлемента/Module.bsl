#Область ОбработчикиСобытийФормы
&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
	
	ЭтаФорма.АвтоЗаголовок = Ложь;
	ЭтаФорма.Заголовок = Объект.Наименование + СтрШаблон(" (на осн. %1)(Контактное лицо)", Объект.ДействуетНаОсновании);     

КонецПроцедуры
# КонецОбласти